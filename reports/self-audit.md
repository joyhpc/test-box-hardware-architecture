# Version 1.1 maintenance record

Evaluator/author: Codex (GPT-6), SELF. Date: 2026-09-14. This is author maintenance review, not independent behavioral evaluation or hardware approval.

The public package now uses anonymous evidence roles and generalized mechanisms. Private source identities, paths, filenames, timestamps and fingerprints are omitted. The source-ingestion helper was removed and replaced by a manifest schema validator. Publication checks reject obvious local paths and private manifest fields, but cannot certify that arbitrary technical prose is safe to disclose; manual content review remains necessary.

Sizing connects production assumptions and allowed concurrent profiles to physical ports, every declared data hop through Host storage, buffer ownership, GT/IO/memory/logic capacity, power and thermal assumptions. The worked example and JSON fixtures preserve the initial capacity shortfalls and a revised arithmetic-feasible candidate. No part, speed grade or hardware result is claimed.

Historical SELF responses and actual earlier failure/retest records are retained with disclosed privacy generalization. The old top-down score is explicitly corrected and a separate sizing rubric dimension added. New unit tests exercise arithmetic, references and failure paths; they are not independent behavioral evidence.

Actual release checks: [verification](verification.json). Packaging/installation scope: [record](installation.json). No historical hardware source project was modified. No existing installed skill is overwritten by maintenance tooling.
