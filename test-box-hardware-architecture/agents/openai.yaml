interface:

  display_name: "Test Box Hardware Architecture"

  short_description: "从需求到模块契约、生命周期、验证闭环的测试盒硬件架构设计"

  default_prompt: "使用 $test-box-hardware-architecture，为下一款测试盒先建立需求、系统边界和模块接口，再分析生命周期与验证。"
